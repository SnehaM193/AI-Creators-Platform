// jsconfig.json
{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      // THIS IS THE MISSING ALIAS for "@components/theme-provider"
      "@components/*": ["components/*"],
      
      // Include the other common alias if your project uses it
      "@/components/*": ["components/*"],
      
      // Include the alias for your utilities
      "@/lib/*": ["lib/*"]
    }
  },
  "exclude": ["node_modules"]
}