// jsconfig.json
{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      // EXISTING ALIASES
      "@components/*": ["components/*"],
      "*/components/": ["components/*"],
      "*/lib/": ["lib/*"],

      // 👇 ADD THIS ENTRY TO RESOLVE @/ to the root directory
      "@/*": ["./*"]
    }
  },
  "exclude": ["node_modules"]
}